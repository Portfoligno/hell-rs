# Generated by `hell-ci protocol render`.
# Source: ci/protocol/v1.toml
# Protocol SHA-256: f6497be3fc4f0eb3e818028d018fdf888b51fd52f77b434a7014b525c3c67866
# Edit the protocol manifest or generator, not this file.
name: CI
on:
  push:
    branches:
      - '**'
    tags-ignore:
      - '**'
  pull_request: {}
permissions:
  actions: read
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
jobs:
  plan:
    name: Bind exact candidate readiness plan
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
      readiness_plan_digest: ${{ steps.plan.outputs.readiness_plan_digest }}
      conformance_plan_digest: ${{ steps.plan.outputs.conformance_plan_digest }}
      trusted_conformance_inputs_digest: ${{ steps.plan.outputs.trusted_conformance_inputs_digest }}
    steps:
      - name: Check out exact candidate readiness driver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: candidate
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-plan-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Restore compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-plan-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/builtins/**', 'automation/compat/**', 'automation/fixtures/**', 'automation/baseline.toml', 'automation/deny.toml', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
      - name: Resolve pinned Rust toolchain
        working-directory: automation
        run: rustup show active-toolchain
      - name: Build readiness driver and independent workflow auditor
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci --package hell-workflow-auditor --bin hell-workflow-auditor
      - name: Render the complete typed workflow protocol
        run: ./automation/target/ci/hell-ci protocol render --manifest candidate/ci/protocol/v1.toml --repository-root candidate --output ci-out/rendered-workflows
      - name: Check exact live workflow protocol bytes
        run: ./automation/target/ci/hell-ci protocol check --manifest candidate/ci/protocol/v1.toml --repository-root candidate --workflows candidate/.github/workflows --report ci-out/protocol-check.json
      - name: Audit live workflow semantics independently
        run: ./automation/target/ci/hell-workflow-auditor audit --workflows candidate/.github/workflows --protocol-projection candidate/ci/protocol/v1.audit.json --action-metadata candidate/ci/actions/metadata-v1.json --output ci-out/workflow-audit.json
      - name: Execute independent workflow protocol vectors
        run: ./automation/target/ci/hell-workflow-auditor verify-vectors --manifest candidate/ci/workflow-vectors/v1/manifest.toml --workflows candidate/.github/workflows --protocol-projection candidate/ci/protocol/v1.audit.json --action-metadata candidate/ci/actions/metadata-v1.json --output ci-out/workflow-vector-audit.json
      - name: Audit exact Rust environment and process capabilities
        run: ./automation/target/ci/hell-ci policy rust-capabilities --repository-root candidate --policy candidate/ci/rust-capabilities.toml --output ci-out/rust-capabilities.json
      - name: Render the release assurance map
        run: ./automation/target/ci/hell-ci assurance render --map candidate/spec/assurance-map.toml --output ci-out/assurance-map.md
      - name: Check release assurance bindings and documentation
        run: ./automation/target/ci/hell-ci assurance check --map spec/assurance-map.toml --repository-root candidate --output ci-out/assurance-check.json
      - name: Create candidate-bound readiness and conformance plans
        id: plan
        run: ./automation/target/ci/hell-ci readiness plan --repository-root candidate --output readiness-plan
      - name: Retain workflow and capability control audits
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-control-audit-${{ github.run_id }}-${{ github.run_attempt }}
          path: ci-out
          if-no-files-found: error
          retention-days: '30'
      - name: Upload exact readiness plan
        id: upload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-plan-${{ github.run_id }}-${{ github.run_attempt }}
          path: readiness-plan
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-plan-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Save compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-plan-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/builtins/**', 'automation/compat/**', 'automation/fixtures/**', 'automation/baseline.toml', 'automation/deny.toml', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
  linux:
    name: Technical readiness / Linux x86_64
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    needs:
      - plan
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Check out exact candidate readiness driver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: candidate
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out pinned upstream oracle source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: chrisdone/hell
          ref: 8e952cf9de4ab25d7716982a9ca234f9bdcf1bff
          path: oracle-source
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Download exact readiness plan
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.plan.outputs.artifact_id }}
          path: readiness-input
      - name: Install pinned Stack
        uses: haskell-actions/setup@6037f33647c3f17758a2356c80fc4a53d7e0685d
        with:
          enable-stack: 'true'
          stack-no-global: 'true'
          stack-version: 3.11.1
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}-cargo-deny-0.20.2
      - name: Restore cargo-deny compilation cache
        id: cargo-deny-target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ci-tool-cache/cargo-deny-target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-cargo-deny-target-0.20.2-${{ hashFiles('automation/rust-toolchain.toml') }}
      - name: Restore candidate compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Restore candidate readiness-driver compilation cache
        id: automation-target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
      - name: Resolve pinned Rust toolchain
        working-directory: automation
        run: rustup show active-toolchain
      - name: Install pinned cargo-deny
        working-directory: automation
        run: cargo install cargo-deny --locked --version 0.20.2 --force --target-dir ../ci-tool-cache/cargo-deny-target
      - name: Build readiness driver
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci --package hell-testkit --bin hell-test-helper
      - name: Run Linux technical readiness gate
        run: ./automation/target/ci/hell-ci readiness platform --platform linux-x86_64 --plan readiness-input/readiness-plan.json --conformance-plan readiness-input/conformance-plan.json --repository-root candidate --oracle-source oracle-source --output platform-out
      - name: Upload exact Linux readiness result
        id: upload
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-linux-${{ github.run_id }}-${{ github.run_attempt }}
          path: platform-out
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}-cargo-deny-0.20.2
      - name: Save cargo-deny compilation cache
        if: ${{ always() && steps.cargo-deny-target-cache.outcome == 'success' && steps.cargo-deny-target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ci-tool-cache/cargo-deny-target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-cargo-deny-target-0.20.2-${{ hashFiles('automation/rust-toolchain.toml') }}
      - name: Save candidate compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Save candidate readiness-driver compilation cache
        if: ${{ always() && steps.automation-target-cache.outcome == 'success' && steps.automation-target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-linux-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
  macos:
    name: Technical readiness / macOS aarch64
    runs-on: macos-15
    timeout-minutes: 180
    needs:
      - plan
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Check out exact candidate readiness driver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: candidate
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out pinned upstream oracle source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: chrisdone/hell
          ref: 8e952cf9de4ab25d7716982a9ca234f9bdcf1bff
          path: oracle-source
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Download exact readiness plan
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.plan.outputs.artifact_id }}
          path: readiness-input
      - name: Install pinned Stack
        uses: haskell-actions/setup@6037f33647c3f17758a2356c80fc4a53d7e0685d
        with:
          enable-stack: 'true'
          ghc-version: 9.8.2
          stack-version: 3.11.1
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}-cargo-deny-0.20.2
      - name: Restore cargo-deny compilation cache
        id: cargo-deny-target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ci-tool-cache/cargo-deny-target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-cargo-deny-target-0.20.2-${{ hashFiles('automation/rust-toolchain.toml') }}
      - name: Restore candidate compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Restore candidate readiness-driver compilation cache
        id: automation-target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
      - name: Resolve pinned Rust toolchain
        working-directory: automation
        run: rustup show active-toolchain
      - name: Expose pinned LLVM archiver
        run: brew link --force llvm@18
      - name: Install pinned cargo-deny
        working-directory: automation
        run: cargo install cargo-deny --locked --version 0.20.2 --force --target-dir ../ci-tool-cache/cargo-deny-target
      - name: Build readiness driver
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci --package hell-testkit --bin hell-test-helper
      - name: Run macOS technical readiness gate
        run: ./automation/target/ci/hell-ci readiness platform --platform macos-aarch64 --plan readiness-input/readiness-plan.json --conformance-plan readiness-input/conformance-plan.json --repository-root candidate --oracle-source oracle-source --output platform-out
      - name: Upload exact macOS readiness result
        id: upload
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-macos-${{ github.run_id }}-${{ github.run_attempt }}
          path: platform-out
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}-cargo-deny-0.20.2
      - name: Save cargo-deny compilation cache
        if: ${{ always() && steps.cargo-deny-target-cache.outcome == 'success' && steps.cargo-deny-target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ci-tool-cache/cargo-deny-target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-cargo-deny-target-0.20.2-${{ hashFiles('automation/rust-toolchain.toml') }}
      - name: Save candidate compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Save candidate readiness-driver compilation cache
        if: ${{ always() && steps.automation-target-cache.outcome == 'success' && steps.automation-target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-macos-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
  windows:
    name: Technical readiness / Windows x86_64
    runs-on: windows-2025
    timeout-minutes: 180
    needs:
      - plan
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Check out exact candidate readiness driver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: candidate
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Check out pinned upstream oracle source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: chrisdone/hell
          ref: 8e952cf9de4ab25d7716982a9ca234f9bdcf1bff
          path: oracle-source
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Download exact readiness plan
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.plan.outputs.artifact_id }}
          path: readiness-input
      - name: Install pinned Stack
        uses: haskell-actions/setup@6037f33647c3f17758a2356c80fc4a53d7e0685d
        with:
          enable-stack: 'true'
          stack-no-global: 'true'
          stack-version: 3.11.1
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}
      - name: Restore candidate compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Restore candidate readiness-driver compilation cache
        id: automation-target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
      - name: Resolve pinned Rust toolchain
        working-directory: automation
        run: rustup show active-toolchain
      - name: Build readiness driver
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci --package hell-testkit --bin hell-test-helper
      - name: Run Windows technical readiness gate
        run: .\automation\target\ci\hell-ci.exe readiness platform --platform windows-x86_64 --plan readiness-input\readiness-plan.json --conformance-plan readiness-input\conformance-plan.json --repository-root candidate --oracle-source oracle-source --output platform-out
      - name: Upload exact Windows readiness result
        id: upload
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-windows-${{ github.run_id }}-${{ github.run_attempt }}
          path: platform-out
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml') }}
      - name: Save candidate compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: candidate-target
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('candidate/rust-toolchain.toml', 'candidate/Cargo.lock', 'candidate/Cargo.toml', 'candidate/crates/**/Cargo.toml', 'candidate/**/build.rs', 'candidate/.cargo/**', 'candidate/crates/**/*.rs', 'candidate/builtins/**', 'candidate/compat/**', 'candidate/fixtures/**', 'candidate/baseline.toml', 'candidate/deny.toml', 'candidate/release-policy.toml', 'candidate/spec/**', 'candidate/.github/**') }}
      - name: Save candidate readiness-driver compilation cache
        if: ${{ always() && steps.automation-target-cache.outcome == 'success' && steps.automation-target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-windows-${{ runner.os }}-${{ runner.arch }}-automation-target-${{ github.sha }}
  verify:
    name: Blocker-zero technical release eligibility
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    needs:
      - plan
      - linux
      - macos
      - windows
    outputs:
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Check out exact candidate readiness verifier
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Download exact readiness plan
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.plan.outputs.artifact_id }}
          path: readiness-input/plan
      - name: Download exact Linux result
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.linux.outputs.artifact_id }}
          path: readiness-input/platforms/linux-x86_64
      - name: Download exact macOS result
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.macos.outputs.artifact_id }}
          path: readiness-input/platforms/macos-aarch64
      - name: Download exact Windows result
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.windows.outputs.artifact_id }}
          path: readiness-input/platforms/windows-x86_64
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-verify-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Restore compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-verify-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/builtins/**', 'automation/compat/**', 'automation/fixtures/**', 'automation/baseline.toml', 'automation/deny.toml', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
      - name: Build readiness verifier
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci
      - name: Reconstruct blocker-zero technical eligibility
        run: ./automation/target/ci/hell-ci readiness verify --plan readiness-input/plan/readiness-plan.json --conformance-plan readiness-input/plan/conformance-plan.json --input readiness-input/platforms --output readiness-result
      - name: Retain exact readiness decision
        id: upload
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-decision-${{ github.run_id }}-${{ github.run_attempt }}
          path: readiness-result
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-verify-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Save compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-verify-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/builtins/**', 'automation/compat/**', 'automation/fixtures/**', 'automation/baseline.toml', 'automation/deny.toml', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
  summary:
    name: Technical release readiness summary
    if: ${{ !cancelled() }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    needs:
      - plan
      - linux
      - macos
      - windows
      - verify
    steps:
      - name: Check out exact candidate readiness summary driver
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          ref: ${{ github.sha }}
          path: automation
          fetch-depth: '1'
          persist-credentials: 'false'
      - name: Restore Cargo downloads
        id: cargo-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-summary-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Restore compilation cache
        id: target-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-summary-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/ci/**', 'automation/compat/**', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
      - name: Build readiness summary driver
        working-directory: automation
        run: cargo build --locked --profile ci --package hell-ci --bin hell-ci
      - name: Record successful plan job
        if: ${{ needs.plan.result == 'success' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job plan --state success --artifact-id ${{ needs.plan.outputs.artifact_id }} --artifact-digest ${{ needs.plan.outputs.artifact_digest }} --output readiness-state
      - name: Record failed plan job
        if: ${{ needs.plan.result == 'failure' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job plan --state failure --output readiness-state
      - name: Record skipped plan job
        if: ${{ needs.plan.result == 'skipped' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job plan --state skipped --output readiness-state
      - name: Record successful Linux job
        if: ${{ needs.linux.result == 'success' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job linux --state success --artifact-id ${{ needs.linux.outputs.artifact_id }} --artifact-digest ${{ needs.linux.outputs.artifact_digest }} --output readiness-state
      - name: Record failed Linux job
        if: ${{ needs.linux.result == 'failure' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job linux --state failure --output readiness-state
      - name: Record skipped Linux job
        if: ${{ needs.linux.result == 'skipped' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job linux --state skipped --output readiness-state
      - name: Record successful macOS job
        if: ${{ needs.macos.result == 'success' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job macos --state success --artifact-id ${{ needs.macos.outputs.artifact_id }} --artifact-digest ${{ needs.macos.outputs.artifact_digest }} --output readiness-state
      - name: Record failed macOS job
        if: ${{ needs.macos.result == 'failure' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job macos --state failure --output readiness-state
      - name: Record skipped macOS job
        if: ${{ needs.macos.result == 'skipped' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job macos --state skipped --output readiness-state
      - name: Record successful Windows job
        if: ${{ needs.windows.result == 'success' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job windows --state success --artifact-id ${{ needs.windows.outputs.artifact_id }} --artifact-digest ${{ needs.windows.outputs.artifact_digest }} --output readiness-state
      - name: Record failed Windows job
        if: ${{ needs.windows.result == 'failure' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job windows --state failure --output readiness-state
      - name: Record skipped Windows job
        if: ${{ needs.windows.result == 'skipped' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job windows --state skipped --output readiness-state
      - name: Record successful verifier job
        if: ${{ needs.verify.result == 'success' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job verify --state success --artifact-id ${{ needs.verify.outputs.artifact_id }} --artifact-digest ${{ needs.verify.outputs.artifact_digest }} --output readiness-state
      - name: Record failed verifier job
        if: ${{ needs.verify.result == 'failure' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job verify --state failure --output readiness-state
      - name: Record skipped verifier job
        if: ${{ needs.verify.result == 'skipped' }}
        run: ./automation/target/ci/hell-ci readiness record-job-state --job verify --state skipped --output readiness-state
      - name: Download exact readiness plan when available
        if: ${{ needs.plan.outputs.artifact_id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.plan.outputs.artifact_id }}
          path: readiness-input/plan
      - name: Download exact Linux result when available
        if: ${{ needs.linux.outputs.artifact_id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.linux.outputs.artifact_id }}
          path: readiness-input/platforms/linux-x86_64
      - name: Download exact macOS result when available
        if: ${{ needs.macos.outputs.artifact_id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.macos.outputs.artifact_id }}
          path: readiness-input/platforms/macos-aarch64
      - name: Download exact Windows result when available
        if: ${{ needs.windows.outputs.artifact_id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.windows.outputs.artifact_id }}
          path: readiness-input/platforms/windows-x86_64
      - name: Download exact verifier result when available
        if: ${{ needs.verify.outputs.artifact_id != '' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          digest-mismatch: error
          artifact-ids: ${{ needs.verify.outputs.artifact_id }}
          path: readiness-input/verifier
      - name: Derive failure-terminal readiness decision
        run: ./automation/target/ci/hell-ci readiness summarize --state-root readiness-state --input readiness-input --output readiness-summary
      - name: Retain failure-terminal readiness summary
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: readiness-summary-${{ github.run_id }}-${{ github.run_attempt }}
          path: readiness-summary
          if-no-files-found: error
          retention-days: '30'
      - name: Save Cargo downloads
        if: ${{ always() && steps.cargo-cache.outcome == 'success' && steps.cargo-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: readiness-summary-${{ runner.os }}-${{ runner.arch }}-cargo-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml') }}
      - name: Save compilation cache
        if: ${{ always() && steps.target-cache.outcome == 'success' && steps.target-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: automation/target
          key: readiness-summary-${{ runner.os }}-${{ runner.arch }}-target-${{ hashFiles('automation/rust-toolchain.toml', 'automation/Cargo.lock', 'automation/Cargo.toml', 'automation/crates/**/Cargo.toml', 'automation/**/build.rs', 'automation/.cargo/**', 'automation/crates/**/*.rs', 'automation/ci/**', 'automation/compat/**', 'automation/release-policy.toml', 'automation/spec/**', 'automation/.github/**') }}
