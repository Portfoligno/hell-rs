main = do
  env <- Environment.getEnvironment
  (out, err) <-
    Text.readProcess_ (
      Process.setEnv (List.cons ("LC_ALL", "C") env)
        (Process.proc "env" [])
    )
  Text.hPutStr IO.stdout out
