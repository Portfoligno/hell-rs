# Compatibility policy

Compatibility is measured at parse, static, pure-semantic, effect, concurrency,
and presentation levels. The pinned executable is a test oracle, not a runtime
dependency. Known baseline quirks are versioned rather than silently corrected.

